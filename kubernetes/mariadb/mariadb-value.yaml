architecture: replication
auth:
  database: url-db
  rootPassword: secret
  username: url
  password: secret

initdbScripts:
  init.sql: |
    CREATE TABLE urls (
      `id` varchar(12) primary key,
      `url` text,
      `expireAt` datetime
    );

primary:
  persistence:
    enable: true
    size: 10Gi

secondary:
  replicaCount: 2
  persistence:
    enable: true
    size: 10Gi

extraEnvVars:
  - name: TZ
    value: "Asia/Taipei"
